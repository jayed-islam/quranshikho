import React from "react";
import { MyCourseView } from "@/src/sections/account/view/my-course-view";

export const metadata = {
  title: "Account: Course Page",
};

const AccountPage = () => {
  return <MyCourseView />;
};

export default AccountPage;
